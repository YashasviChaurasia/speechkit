let asset, artifact; const $ = selector => document.querySelector(selector);
const esc = value => String(value ?? "").replace(/[&<>"']/g, character => ({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"})[character]);
const time = seconds => `${Math.floor(seconds / 60)}:${String(Math.floor(seconds % 60)).padStart(2,"0")}`;
function seek(seconds) { const media=$("#media"); media.currentTime=seconds; media.play().catch(()=>{}); }
function tags(values, cls="") { return values?.length ? `<span class="tags ${cls}">${values.map(value=>`<i>${esc(value)}</i>`).join("")}</span>` : ""; }
function render() {
  $("#result").hidden=false; $("#media").src=`/api/assets/${asset}/media`; $("#filename").textContent=artifact.filename;
  const meta=artifact.metadata||{};
  $("#facts").innerHTML=[["Model",artifact.model],["Language",artifact.language_code||"Unknown"],["Speakers",artifact.speakers.length],["Turns",artifact.segments.length],["Duration",time(artifact.duration_seconds)],["Est. cost",`₹${meta.estimated_cost_inr ?? "—"}`],["Status","Complete"]].map(([key,value])=>`<div><dt>${key}</dt><dd>${esc(value)}</dd></div>`).join("");
  $("#speaker-cards").innerHTML=artifact.speakers.map(speaker=>`<article class="speaker-card"><div class="speaker-name"><input aria-label="Speaker name" value="${esc(speaker.display_name)}" data-speaker="${esc(speaker.speaker_id)}"><button class="rename" data-speaker="${esc(speaker.speaker_id)}">Save</button></div><p><strong>${speaker.speaking_percentage}%</strong> · ${time(speaker.speaking_seconds)} speaking</p><small>${speaker.turn_count} turns · ${speaker.word_count} words · ${speaker.questions_asked} questions</small>${tags(speaker.keywords)}${tags(speaker.entities,"entities")}<blockquote>${esc(speaker.representative_quotes[0]||"No representative quote")}</blockquote></article>`).join("");
  $("#segments").innerHTML=artifact.segments.map(segment=>`<button class="segment" data-start="${segment.start_seconds}"><span class="time">${time(segment.start_seconds)}–${time(segment.end_seconds)}</span><span><b>${esc(segment.speaker_name)}</b><em>${esc(segment.text)}</em>${tags(segment.keywords)}${tags(segment.entities,"entities")}</span></button>`).join("");
  $("#raw").textContent=JSON.stringify(artifact,null,2);
}
async function refresh(){ artifact=await (await fetch(`/api/assets/${asset}/artifact`)).json(); render(); }
async function rename(button){ const id=button.dataset.speaker, name=button.previousElementSibling.value.trim(); if(!name)return; await fetch(`/api/assets/${asset}/speakers/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({display_name:name})}); await refresh(); if($("#query").value) search(); }
function resultCard(result){ const close=!result.is_exact&&result.similarity!=null, field={keywords:"keyword",entities:"entity",topics:"topic",speaker_name:"speaker"}[result.matched_fields?.[0]]||"token"; const details=close?`<p class="close-match"><strong>Close ${field} · ${Math.round(result.similarity*100)}%</strong><br>Query: ${esc($("#query").value.trim())} · Matched term: ${esc(result.matched_term)} · Field: ${esc(result.matched_fields?.join(", "))}</p>`:""; return `<article class="result" data-start="${result.start_seconds}"><div><span class="time">${time(result.start_seconds)}–${time(result.end_seconds)}</span><b>${esc(result.speaker_name)}</b></div>${details}<p>${esc(result.text)}</p>${tags(result.keywords)}${tags(result.entities,"entities")}<footer><span>score ${result.score}</span><span>matched: ${esc(result.matched_fields?.join(", ")||"indexed fields")}</span><button class="play" data-start="${result.start_seconds}">Play from timestamp</button></footer></article>`; }
async function search(){ const query=$("#query").value.trim(), mode=$("#mode").value; if(!query)return; const response=await fetch(`/api/assets/${asset}/search?q=${encodeURIComponent(query)}&mode=${mode}`); const data=await response.json(); if(!response.ok){$("#search-meta").textContent=data.detail||"Search failed";return;} $("#search-meta").textContent=`${data.results.length} result${data.results.length===1?"":"s"} · ${data.elapsed_ms} ms · ${data.mode}`; $("#results").innerHTML=data.results.map(resultCard).join("")||"<p class='empty'>No indexed match. Try substring mode for a middle-of-word query.</p>"; }
$("#upload").onsubmit=async event=>{event.preventDefault();$("#status").textContent="Extracting audio, submitting Sarvam Batch STT, and indexing turns…"; const response=await fetch("/api/assets",{method:"POST",body:new FormData(event.target)}),data=await response.json();if(!response.ok){$("#status").textContent=data.detail;return;}asset=data.asset_id;await refresh();$("#status").textContent="Complete";};
$("#search").onclick=search; $("#query").onkeydown=event=>{if(event.key==="Enter"){event.preventDefault();search();}};
document.addEventListener("click",event=>{const renameButton=event.target.closest(".rename"),start=event.target.closest("[data-start]");if(renameButton)rename(renameButton);if(start && !event.target.closest("input"))seek(Number(start.dataset.start));});
$("#export").onclick=()=>{const anchor=document.createElement("a");anchor.href=URL.createObjectURL(new Blob([JSON.stringify(artifact,null,2)],{type:"application/json"}));anchor.download="speechkit.v1.json";anchor.click();};
const preloaded = new URLSearchParams(location.search).get("asset");
if (preloaded) { asset=preloaded; refresh().then(()=>$("#status").textContent="Loaded completed demo").catch(()=>$("#status").textContent="The requested demo asset is unavailable."); }
